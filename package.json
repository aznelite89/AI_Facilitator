{
  "name": "ai-facilitator-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -k -n server,client \"pnpm -C server dev\" \"pnpm -C client dev\"",
    "dev:win": "concurrently -k -n server,client \"pnpm -C server dev\" \"pnpm -C client dev\"",
    "build": "pnpm -C client build",
    "start": "pnpm -C server start",
    "lint": "pnpm -C server lint && pnpm -C client lint"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
